name: dq_monitor
version: "1.0.0"
config-version: 2

profile: dq_monitor

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dq_monitor:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
